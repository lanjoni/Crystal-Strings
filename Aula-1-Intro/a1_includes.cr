text = "The black cat climbed the green three"

puts text.includes?("cat") # Retorna true

puts text.includes?("dog") # Retorna false

# Adicionando "includes?" seguindo da sequência de caracteres que queremos verificar, podemos saber se a string contém a determinada sequência ou não, retornando true ou false
